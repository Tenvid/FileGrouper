[project]
name = "file-grouper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyfakefs>=5.10.0",
]

[dependency-groups]
dev = [
    "behave>=1.3.3",
    "mypy>=1.18.2",
    "ruff>=0.14.1",
]
